export type AddressIp = {
    
    id: number;         
    network_id: number;
    ip: string;         
    network_ip: string;           
    gatway?: string;
    dns_one?: string;
    dns_two?: string;   

}